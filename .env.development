# Yggleaf Frontend 环境配置

# AJAX API 基础地址（本地走 Vite proxy → http://localhost:5577）
VITE_API_BASE_URL=/api-auth/api/v1

# MC 服务 API 基础地址（本地走 Vite proxy → http://localhost:5599）
VITE_MC_API_BASE_URL=/api-mc/api/v1

# SSO 登录跳转地址（浏览器直接导航，不走 proxy）
VITE_SSO_BASE_URL=http://localhost:5577/api/v1
